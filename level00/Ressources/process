chercher tout les ficheirs accessibles par level00 avec
ls -laR / 2>/dev/null | grep -e : -e flag00 | grep -C 1 flag00
il y en a deux, /rofs/usr/sbin/john et /sr/sbin/john
quand on cat ca donne "cdiiddwpgswtgt"
on teste avec plusieurs clefs et avec un decalage et au final avec un rot15 ca donne "nottoohardhere"
